// ! don't worry about compiling this function for now
function add(a, b) {
    return a + b
}

// ! do not worry about compiling the function for now
function log(text) {
    console.log(text)
}

// call expression with input and output
log(add(1, 2))
// now try with variable declaration
const res = add(1, 2)
log(res)