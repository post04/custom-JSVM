// ! do not worry about compiling the function for now
function log(text) {
    console.log(text)
}

// single call expression, no member expression here so may be easier for beginning
log()
log("test")