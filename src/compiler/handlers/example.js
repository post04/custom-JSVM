const t = require('@babel/types');
const {
    Opcodes
} = require('../../constants');

/*
    EXAMPLE FILE - DON'T MODIFY
*/

module.exports = (path, node, index, handler) => {
    let bytes = []
    let order = []

    // do stuff 

    return [order, bytes]
}